<!DOCTYPE html>
<html>

<head>
  <style>
    .grid-container {
      display: grid;
      grid-template-columns: auto auto auto auto auto;
      background-color: #0c5794;
      padding: 10px;
    }

    .grid-item {
      background-color: rgba(255, 255, 255);
      border: 1px solid rgba(0, 0, 0, 0.8);
      padding: 20px;
      font-size: 40px;
      font-weight: bold;
      height: 100px;
      line-height: 100px;
      text-align: center;
    }

    span {
      display: inline-block;
      vertical-align: middle;
      line-height: normal;
    }
  </style>
</head>

<body>

  <h1>Olá, {{ player }}!</h1>

  <div class="grid-container">
    {% for value in first_card_values %}
    <div class="grid-item">{{value}}</div>
    {% endfor %}

    <div class="grid-item">
      <img src="../static/indiozim.png" alt="indiozim" style="width:100px;height:100px;">
    </div>

    {% for value in second_card_values %}
    <div class="grid-item">{{value}}</div>
    {% endfor %}

  </div>

</body>

</html>